import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "center"
    },
    headerTitle: {
        color: '#000',
        fontSize: 20,
        fontFamily: 'SansitaSwashed-Black'
    }
});

export default styles;